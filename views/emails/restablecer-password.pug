<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
    h2 Restablecer Password
    p Hola has solicitado restablecer tu Password, haz click aquí
    a(href=`${resetUrl}`) Ir a Resetear el Password
    p si no puedes acceder al enlace visita #{resetUrl}
    p si no solicitaste este email, por favor ignorar.