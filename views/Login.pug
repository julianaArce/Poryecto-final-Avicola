<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=, initial-scale=1.0")
        link(rel="stylesheet", href="/materialize/css/materialize.min.css")
        link(rel="stylesheet", href="/materialize/css/UsuariosNuevos.css")
        link(rel="stylesheet", href="/materialize/css/cambios.css")
        link(rel="stylesheet", href="/css/app.css")
        link(href='https://fonts.googleapis.com/icon?family=Material+Icons', rel='stylesheet')
        title Iniciar Sesión
    body
    body.fondo-registro 
        nav.nav-wrapper.menu
            a.brand-logo(href='#')  
            ul#nav-mobile.right.hide-on-med-and-down
                li
                    a(href="/" ) volver a la Pagina Inicio
        section.inicio-registro
        center
            .card-panel.registro-panel
                .contenido-registro
                    h1 INICIAR SESIÓN
                    if locals.mensajes
                        .alertas
                            each categoria in Object.keys(locals.mensajes)
                                each error in mensajes[categoria]
                                    .alerta(class=`${categoria}`)
                                        p=error
                    form(method= "POST" action="/Login")
                        .row
                            .input-field
                                input#email.validate(type='email' name ='emailUsuario' value=emailUsuario)
                                label(for='email') Correo Electronico
                        .row
                            .input-field
                                input#password.validate(type='password' name ='passwordUsuario' )
                                label(for='password') Contraseña
                        .row
                            .input-field
                                a(href="/UsuariosNuevos") Crear cuenta
                            .input-field
                                a(href="/restablecer") Restaurar contraseña
                    
                        button.waves-effect.waves-light.btn-large( type='submit' name='action' style='background-color: red;') INICIAR SESIÓN
    script(src="/materialize/js/materialize.min.js") 
    script(src="/materialize/js/jquery-3.5.1.min.js")          
    block contenido
        